plugin: aws_ec2
regions:
  - us-east-1
filters:
  tag:Name: devops-training-ec2
  instance-state-name: running
keyed_groups:
  - key: tags.Environment
    prefix: env
hostnames:
  - private-ip-address
compose:
  ansible_host: private_ip_address